# ADR 目录说明

- 本目录用于存放架构决策记录（Architecture Decision Record）。
- 文件命名规则：`[四位流水号]-[snake_case_slug].md`。
- 每份 ADR 必须包含以下四个章节：
  - `背景 (Context)`
  - `考虑过的方案 (Options)`
  - `最终决策 (Decision)`
  - `影响 (Consequences)`
- 决策文档不得再散落在 `lib/**/doc` 下。
